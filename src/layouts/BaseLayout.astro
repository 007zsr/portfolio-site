---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { getLocaleFromPath, htmlLang, type Locale } from '../lib/i18n';
import { getLocalizedPath, getSeoMeta } from '../lib/content-api';
import { getRouteKeyFromPath } from '../lib/routes';
import { absoluteUrl, pageTitle, siteConfig, siteName } from '../lib/site';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  type?: string;
  locale?: Locale;
}

const {
  title,
  locale = getLocaleFromPath(Astro.url.pathname),
  description,
  image,
  type = 'website'
} = Astro.props;

const routeKey = getRouteKeyFromPath(Astro.url.pathname);
const seo = getSeoMeta(routeKey, locale);
const canonicalUrl = absoluteUrl(Astro.url.pathname);
const resolvedTitle = pageTitle(title ?? seo.title, locale);
const resolvedDescription = description ?? seo.description;
const ogImage = absoluteUrl(image ?? seo.image);
const alternateLocale = locale === 'zh' ? 'en' : 'zh';
const alternatePath = getLocalizedPath(Astro.url.pathname, alternateLocale);
---

<!doctype html>
<html lang={htmlLang(locale)}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>{resolvedTitle}</title>
    <meta name="description" content={resolvedDescription} />
    <link rel="canonical" href={canonicalUrl} />
    <link rel="alternate" hreflang={htmlLang(locale)} href={canonicalUrl} />
    <link rel="alternate" hreflang={htmlLang(alternateLocale)} href={absoluteUrl(alternatePath)} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta property="og:locale" content={siteConfig.ogLocale[locale]} />
    <meta property="og:site_name" content={siteName(locale)} />
    <meta property="og:title" content={resolvedTitle} />
    <meta property="og:description" content={resolvedDescription} />
    <meta property="og:type" content={type} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={ogImage} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={resolvedTitle} />
    <meta name="twitter:description" content={resolvedDescription} />
    <meta name="twitter:image" content={ogImage} />
  </head>
  <body>
    <div class="site-shell">
      <Header locale={locale} pathname={Astro.url.pathname} />
      <main>
        <slot />
      </main>
      <Footer locale={locale} />
    </div>
  </body>
</html>
