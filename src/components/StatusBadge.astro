---
interface Props {
  status: string;
}

const { status } = Astro.props;
const variant = status.toLowerCase().replaceAll(' ', '-');
---

<span class:list={['status-badge', `status-${variant}`]}>{status}</span>
